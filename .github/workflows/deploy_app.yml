# name : Deploy Streamlit App

# on:
#   push:
#     branches: [ main ]

# jobs:
#   deploy:
#     runs-on: ubuntu-latest
#     steps:
#       - name: checkout code
#         uses: actions/checkout@v3
      
#       - name: setup python
#         uses: actions/setup-python@v3
#         with:
#           python-version: '3.13'
      
#       - name: Install Dependencies
#         run: |
#           python -m pip install --upgrade pip
#           pip install pipenv
#           cd diagnostics
#           pipenv install
#           pipenv run streamlit run app.py > /dev/null
      
#       # - name: Deploy Streamlit App to Server
#       #   run: |
#       #     pipenv run streamlit run app.py
